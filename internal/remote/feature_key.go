package remote

// OverlayFeatureKey is the stable overlay registration ID for remote UI (add remote, auth).
const OverlayFeatureKey = "remote"

// Overlay open request keys ([inputlifecycletype.OverlayPayload.Key]) for remote flows.
const OverlayOpenKeyAddRemote = "remote_add"
