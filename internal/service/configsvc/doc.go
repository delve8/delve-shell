// Package configsvc loads/saves LLM-related settings from config.yaml, validates fields, and runs connectivity checks.
// It is the heavier application service under internal/service; remotesvc and skillsvc are thinner facades.
package configsvc
